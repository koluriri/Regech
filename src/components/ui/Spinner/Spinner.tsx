/* eslint-disable react/require-default-props */
import { FC } from 'react';
import styles from './Spinner.module.css';

const Spinner: FC = () => <div className={styles.spinner} />;

export default Spinner;
