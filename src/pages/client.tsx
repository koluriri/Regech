import { NextPage } from 'next';

const Client: NextPage = () =>
  /* const trpc = useHello();

  trpc.hello.query({text:'hello'}).then((data)=>{});

  return (
    <div>
      <div>{data?.greeting}</div>
    </div>
  ); */

  null;
export default Client;
